# Rendered by ansible (claude-code role). Do not edit by hand —
# changes will be overwritten on the next playbook run.
#
# Sourced from ~/.bashrc to provision Claude Code's headless OAuth
# token. Generated with `claude setup-token` on an interactive machine.
export CLAUDE_CODE_OAUTH_TOKEN="{{ claude_code_oauth_token }}"
